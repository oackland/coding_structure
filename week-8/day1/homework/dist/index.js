"use strict";
let song = {
    title: "Lose Yourself",
    releaseYear: 1993,
};
console.log(`this is working\n ${song.title} ${song.releaseYear}`);
let prices = [100, 200, 300];
prices[0] = "$100";
function myFunc(a, b) {
    return a;
}
